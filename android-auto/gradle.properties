org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true

# Default backend baked into BuildConfig.DEFAULT_BACKEND_URL. Without this the app
# starts with an empty backend URL, so every host segment and YouTube stream fails
# with "This device has not been enrolled" and surfaces as "Connection lost".
# Override per build with -PMIRAI_BACKEND_URL=https://...
MIRAI_BACKEND_URL=https://radio-ai-backend-dktu4p5zqq-as.a.run.app
